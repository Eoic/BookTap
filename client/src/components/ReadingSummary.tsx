import * as React from 'react';

export interface IReadingSummaryProps {
}

export default class ReadingSummary extends React.Component<IReadingSummaryProps> {
  public render() {
    return (
      <div>
        Summary
      </div>
    );
  }
}
