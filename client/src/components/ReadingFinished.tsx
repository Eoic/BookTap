import * as React from 'react';

export interface IReadingFinishedProps {
}

export default class ReadingFinished extends React.Component<IReadingFinishedProps> {
  public render() {
    return (
      <div>
        Finished
      </div>
    );
  }
}
